#INCLUDE 'PROTHEUS.CH'
#INCLUDE 'PARMTYPE.CH'

//-----------------------------------------------------------------------
/*/{Protheus.doc} FA740BRW()

Adição de itens no menu da mBrowse

O ponto de entrada FA740BRW foi desenvolvido para adicionar itens no menu 
da mBrowse. Retorna array com os novas opções e manda como parâmetro o 
array com as opções padrão.

@param		aRotOrig = Array contendo os botões
@return		aRotina = Array contendo os novos botões
@author 	Fabio Cazarini
@since 		22/07/2016
@version 	1.0
@project	MAN0000001 - Aguassanta - Integra
/*/
//-----------------------------------------------------------------------
USER FUNCTION FA740BRW()
	LOCAL aRotOrig := PARAMIXB

//User Function FA740BRW()Local aBotao := {}Return(aBotao
	
	//-----------------------------------------------------------------------
	// Adiciona botão para executar a transferência do portador e situação em 
	// lote
	//-----------------------------------------------------------------------
	aRotina := U_ASFINA41({})
	
RETURN aRotina